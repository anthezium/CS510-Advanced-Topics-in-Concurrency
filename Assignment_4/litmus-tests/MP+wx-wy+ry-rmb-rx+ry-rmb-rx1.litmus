C MP+wx-wy+ry-rmb-rx+ry-rmb-rx1

{}

P0(int *x, int *y)
{
	WRITE_ONCE(*x, 1);
	WRITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int r0;
	int r1;

	r0 = READ_ONCE(*y);
  smp_rmb();
	r1 = READ_ONCE(*x);
}

P2(int *x, int *y)
{
	int r2;
	int r3;

	r2 = READ_ONCE(*y);
  smp_rmb();
	r3 = READ_ONCE(*x);
}

exists (1:r0=1 /\ 1:r1=0 /\ 2:r2=0 /\ 2:r3=1)
